/**
 * Position and Layout Types
 */

export type SnapPosition = 'top' | 'bottom' | 'left' | 'right'
export type HudPos = { left: number; top: number }
export type HudSize = { width: number; height: number }
